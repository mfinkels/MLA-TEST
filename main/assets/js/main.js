/*
window.onload = function() {
  window.addEventListener('resize', adjustHeaderMain);
}

function adjustHeaderMain() {
  var height = document.getElementsByTagName("header").offsetHeight;
  document.getElementsByTagName("main").style.marginTop = height + 'px';

  var page = $(window).height();
  var minHeight = page - height;
  document.getElementById("contenido").style.minHeight  = minHeight + 'px';
}
*/
